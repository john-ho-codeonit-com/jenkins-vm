#cloud-config
# TODO:
# - init script once
#   - create user
#   - create API token
# - run script
# - install plugins (iterate json file remotely)

package_upgrade: true
runcmd:
  - wget -q -O - https://jenkins-ci.org/debian/jenkins-ci.org.key | apt-key add -
  - apt-add-repository "deb https://pkg.jenkins.io/debian-stable binary/"
  - apt-get update && apt-get install jenkins docker.io openjdk-8-jre -y
  - echo 'JAVA_ARGS="-Djenkins.install.runSetupWizard=false"' >> /etc/default/jenkins
  - usermod -aG docker jenkins
  - mkdir -p /var/lib/jenkins/init.groovy.d
  - curl https://raw.githubusercontent.com/john-ho-codeonit-com/jenkins-vm/master/init.groovy -o /var/lib/jenkins/init.groovy.d/init.groovy
  - service jenkins restart